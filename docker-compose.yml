services:

  frontend:
    build: ./nadsoft-frontend
    container_name: nadsoft-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ./nadsoft-backend
    container_name: nadsoft-backend
    ports:
      - "5000:5000"
    depends_on:
      mysql:
        condition: service_healthy
    env_file:
      - ./nadsoft-backend/.env
    environment:
      - DB_HOST=mysql

  mysql:
    image: mysql:8
    container_name: nadsoft-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: nadsoft
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  mysql-data: